import {
  Home,
  Wind,
  TrendingUp,
  GitFork,
  RotateCw,
  Sigma,
  Settings,
  Info,
  Lock,
} from 'lucide-react';

/**
 * modules.js declara íconos como strings (datos puros, serializables).
 * Este mapa es la única frontera donde esos strings se convierten en
 * componentes React reales.
 */
export const ICONS = {
  Home,
  Wind,
  TrendingUp,
  GitFork,
  RotateCw,
  Sigma,
  Settings,
  Info,
  Lock,
};
